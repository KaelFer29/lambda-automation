name: CI/CD Lambda

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  id-token: write
  contents: read

env:
  TF_INPUT: "false"

jobs:
  deploy:
    name: Plan/Apply Lambda
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: gha-opentofu

      - name: Init
        working-directory: infra
        run: tofu init

      - name: Validate
        working-directory: infra
        run: tofu validate

      - name: Import existing IAM role (idempotent)
        working-directory: infra
        run: |
          # Importa el rol si ya existe para evitar conflicto de estado local
          tofu import aws_iam_role.lambda_exec hello-lambda-exec || true

      - name: Import existing Lambda (optional)
        working-directory: infra
        run: |
          # Importa la Lambda si ya existe; ignora si no está creada aún
          tofu import aws_lambda_function.this hello-lambda || true

      - name: Plan
        if: github.event_name == 'pull_request'
        working-directory: infra
        run: tofu plan -no-color

      - name: Plan (save)
        if: github.event_name == 'push'
        working-directory: infra
        run: tofu plan -out plan.out -no-color

      - name: Apply to main
        if: github.event_name == 'push'
        working-directory: infra
        run: tofu apply -auto-approve plan.out
